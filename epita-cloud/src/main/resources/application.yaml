spring:
  application:
    name: epita-cloud

aws:
  dynamodb:
    access-key: ${AWS_ACCESS_KEY_ID:}
    secret-key: ${AWS_SECRET_ACCESS_KEY:}
    endpoint: ${DYNAMODB_ENDPOINT:}
    region: ${AWS_REGION:eu-west-3}

jwt:
  secret: ${JWT_SECRET:epita-cloud-super-secret-key-change-me-please-32bytes}
